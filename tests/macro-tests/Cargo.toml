[package]
name = "macro-tests"
version = "0.0.0"
edition = "2021"
publish = false

[lib]

[features]
default = []
test-feature = []
placebo-test-feature = []

[dev-dependencies]
tryexpand = { path = "../../" }
