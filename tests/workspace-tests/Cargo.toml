[workspace]
members = ["./member-tests"]
exclude = ["./excluded-tests"]
resolver = "2"

[package]
name = "root-tests"
version = "0.0.0"
edition = "2021"

[lib]

[dev-dependencies]
tryexpand = { path = "../../" }
