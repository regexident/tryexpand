[package]
name = "other-tests"
version = "0.0.0"
edition = "2021"
publish = false

[lib]

[dependencies]
uuid = "1.6.1"
http = { version = "0.2", optional = true }

[dev-dependencies]
tryexpand = { path = "../../" }

[features]
default = []
test-feature = ["dep:http"]
placebo-test-feature = []
