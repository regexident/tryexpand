[package]
name = "other-tests"
version = "0.0.0"
edition = "2021"
publish = false

[lib]

[dev-dependencies]
tryexpand = { path = "../../" }

[features]
default = []
test-feature = []
placebo-test-feature = []
